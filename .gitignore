node_modules/

# local tools and credentials
.claude/
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore

# build output
out/
dist/
*.vsix
# esbuild's bundle of media/stage.js + the foliate graph it imports. It has to
# ship (the device frame cannot load a URL), but `npm run bundle` regenerates it.
media/stage.bundle.js

# produced by the export command when run against the test fixture
test/fixtures/*.epub

# vscode extension testing
.vscode-test/

# logs
*.log
npm-debug.log*

# playwright-mcp scratch output, and the screenshots it drops in the root
.playwright-mcp/
/*.png

# generated annotation sidecars for project documentation
docs/*.md.notes.json

# regenerated by `npm run spike:fixture` / `spike/export-sample.mjs`
spike/fixture.epub
spike/exported.epub

# os
.DS_Store
Thumbs.db
spike/exported-lv1.epub
spike/rebuild-*.epub
spike/notes.epub
spike/reader-experience.epub
